rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Patients can read and write their own files only.
    match /patients/{patientId}/{allPaths=**} {
      allow read, write: if request.auth != null
          && request.auth.uid == patientId;
    }
  }
}
